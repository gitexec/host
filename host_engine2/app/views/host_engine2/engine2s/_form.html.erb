<%= form_for(@engine2) do |f| %>
  <% if @engine2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engine2.errors.count, "error") %> prohibited this engine2 from being saved:</h2>

      <ul>
      <% @engine2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
