<%= form_for(@engine1) do |f| %>
  <% if @engine1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engine1.errors.count, "error") %> prohibited this engine1 from being saved:</h2>

      <ul>
      <% @engine1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
